# Daryn Ustaz course sites

Fourteen independent static course sites and one FastAPI reviews backend. Reviews are isolated by course slug and stored in PostgreSQL in production.

Build frontends with `python build_sites.py` after setting `PUBLIC_API_URL`.
