# Daryn Ustaz course sites

Six independent static course sites and one FastAPI reviews backend. Reviews are isolated by course slug and stored in PostgreSQL in production.

Build frontends with `powershell -ExecutionPolicy Bypass -File build_sites.ps1` after setting `PUBLIC_API_URL`.
